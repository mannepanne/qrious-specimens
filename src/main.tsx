// ABOUT: React application entry point
// ABOUT: QueryClientProvider lives in App.tsx; main.tsx only mounts the root
import React from 'react'
import ReactDOM from 'react-dom/client'
import { App } from './App'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)
